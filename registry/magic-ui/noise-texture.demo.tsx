import { NoiseTexture } from "@/registry/magic-ui/noise-texture";

export default function Demo() {
  return (
    <div className="relative h-[600px] w-full overflow-hidden bg-background">
      <NoiseTexture />
    </div>
  );
}
