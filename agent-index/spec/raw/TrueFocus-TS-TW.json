{
  "name": "TrueFocus-TS-TW",
  "blurb": "Words blur and sharpen in sequence with a glowing corner-bracket highlight cycling through them.",
  "description": "Splits text into words and cycles focus: the focused word is sharp, others blur. A glowing corner-bracket element animates to each focused word. Can cycle automatically or respond to hover in manual mode.",
  "use_when": "A headline wants to emphasize words one at a time with visual hierarchy and a sci-fi aesthetic.",
  "avoid_when": "When all words need to be readable simultaneously or when constant motion might distract.",
  "a11y": "All words remain in the DOM; blur is visual only. Readable in any order.",
  "props": [
    {"name": "sentence", "type": "string", "default": "True Focus", "required": false},
    {"name": "separator", "type": "string", "default": " ", "required": false},
    {"name": "manualMode", "type": "boolean", "default": "false", "required": false},
    {"name": "blurAmount", "type": "number", "default": "5", "required": false},
    {"name": "borderColor", "type": "string", "default": "green", "required": false},
    {"name": "glowColor", "type": "string", "default": "rgba(0, 255, 0, 0.6)", "required": false},
    {"name": "animationDuration", "type": "number", "default": "0.5", "required": false},
    {"name": "pauseBetweenAnimations", "type": "number", "default": "1", "required": false}
  ],
  "traits": {
    "trigger": ["always", "hover"],
    "motion": "pronounced",
    "affects": ["text"],
    "tone": "dramatic",
    "theme": "both",
    "needs_sized_parent": false
  },
  "slot_hints": ["headline"]
}
