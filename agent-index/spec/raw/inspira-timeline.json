{
  "name": "inspira-timeline",
  "blurb": "Vertical timeline with scroll-triggered progress line filling from top to bottom.",
  "description": "Timeline component displaying a list of events vertically. Each event has a label (e.g., year) and content area. A vertical line on the left animates upward as the user scrolls, driven by Framer Motion's useScroll. Sticky labels align on the left; content floats on the right.",
  "use_when": "Company history, product roadmaps, or event chronologies where scroll reveals progression.",
  "avoid_when": "Many timeline items (50+) — rendering performance and scroll tracking can suffer.",
  "a11y": "Semantic h2/h3 headings. Timeline line is decorative. No keyboard interaction — content must be scrollable.",
  "props": [
    {"name": "items", "type": "TimelineItem[]", "default": "[]", "required": false},
    {"name": "title", "type": "string", "default": null, "required": false},
    {"name": "description", "type": "string", "default": null, "required": false},
    {"name": "containerClass", "type": "string", "default": null, "required": false},
    {"name": "className", "type": "string", "default": null, "required": false}
  ],
  "traits": {
    "trigger": ["scroll"],
    "motion": "pronounced",
    "affects": ["fill"],
    "tone": "minimal",
    "theme": "both",
    "needs_sized_parent": false
  },
  "slot_hints": ["body-text", "feature-card"]
}
