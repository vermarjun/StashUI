{
  "name": "comp-11",
  "blurb": "URL input with 'https://' prefix text overlay.",
  "description": "Input with absolutely positioned 'https://' text at start (inline, non-interactive).",
  "use_when": "URL field where you want to show but not require 'https://' prefix.",
  "avoid_when": "No validation logic.",
  "a11y": "Prefix is pointer-events-none (doesn't interfere with input).",
  "props": [],
  "traits": {
    "trigger": ["always"],
    "motion": "none",
    "affects": ["text"],
    "tone": "minimal",
    "theme": "both",
    "needs_sized_parent": false
  },
  "slot_hints": ["form-field"]
}
