{
  "name": "Dither-TS-TW",
  "blurb": "Dithered wave pattern with retro pixelated color quantization.",
  "description": "React Three Fiber scene with FBM wave animation combined with Bayer dithering post-processing. Supports wave color, speed, frequency, pixel size, and optional mouse interaction. Uses Effect Composer for post-processing.",
  "use_when": "Retro or lo-fi aesthetic backgrounds where dithering and color reduction create intentional visual degradation.",
  "avoid_when": "High-fidelity designs or when smooth animations are required; dithering creates visible banding.",
  "a11y": "Canvas-based, no semantic content. Mouse interaction is optional. No prefers-reduced-motion guard.",
  "props": [
    {"name": "waveSpeed", "type": "number", "default": "0.05", "required": false},
    {"name": "waveFrequency", "type": "number", "default": "3", "required": false},
    {"name": "waveAmplitude", "type": "number", "default": "0.3", "required": false},
    {"name": "waveColor", "type": "[number, number, number]", "default": "[0.5, 0.5, 0.5]", "required": false},
    {"name": "colorNum", "type": "number", "default": "4", "required": false},
    {"name": "pixelSize", "type": "number", "default": "2", "required": false},
    {"name": "disableAnimation", "type": "boolean", "default": "false", "required": false},
    {"name": "enableMouseInteraction", "type": "boolean", "default": "true", "required": false},
    {"name": "mouseRadius", "type": "number", "default": "1", "required": false}
  ],
  "traits": {
    "trigger": ["always", "pointer-move"],
    "motion": "continuous",
    "affects": ["background"],
    "tone": "playful",
    "theme": "both",
    "needs_sized_parent": true
  },
  "slot_hints": ["page-background", "section-background"]
}
