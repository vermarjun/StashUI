{
  "name": "lamp",
  "blurb": "Animated overhead lamp effect with expanding conic gradients and light rays.",
  "description": "Full-screen container with animated conic gradient layers creating a glowing lamp effect. Multiple masked gradients and blurs create depth and luminosity.",
  "use_when": "A striking, full-screen hero background with a dramatic, illuminated aesthetic.",
  "avoid_when": "Limited viewport height — component requires min-h-screen. Performance may be impacted by multiple simultaneous blur and gradient animations.",
  "a11y": "Full-screen wrapper with content slot (z-50); focus and keyboard behaviour come from children.",
  "props": [
    {"name": "children", "type": "React.ReactNode", "default": null, "required": false},
    {"name": "className", "type": "string", "default": null, "required": false}
  ],
  "traits": {
    "trigger": ["always"],
    "motion": "continuous",
    "affects": ["background"],
    "tone": "dramatic",
    "theme": "dark",
    "needs_sized_parent": false
  },
  "slot_hints": ["page-background", "hero"]
}
