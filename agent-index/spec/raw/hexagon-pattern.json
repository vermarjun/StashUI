{
  "name": "hexagon-pattern",
  "blurb": "Honeycomb hexagon grid with optional highlighting and orientation.",
  "description": "SVG pattern overlay rendering a tessellated hexagon grid. Supports flat-top (horizontal) or pointy-top (vertical) orientation. Can highlight specific hexagons via [col,row] coordinates.",
  "use_when": "A sophisticated static hexagon background that needs directional control and selective highlighting.",
  "avoid_when": "Many highlighted hexagons — each is a separate SVG polygon element.",
  "a11y": "Marked aria-hidden with pointer-events-none; purely decorative overlay.",
  "props": [
    {"name": "radius", "type": "number", "default": "40", "required": false},
    {"name": "gap", "type": "number", "default": "0", "required": false},
    {"name": "x", "type": "number", "default": "-1", "required": false},
    {"name": "y", "type": "number", "default": "-1", "required": false},
    {"name": "strokeDasharray", "type": "string", "default": "'0'", "required": false},
    {"name": "direction", "type": "'horizontal' | 'vertical'", "default": "'horizontal'", "required": false},
    {"name": "hexagons", "type": "Array<[col: number, row: number]>", "default": null, "required": false},
    {"name": "className", "type": "string", "default": null, "required": false}
  ],
  "traits": {
    "trigger": ["always"],
    "motion": "none",
    "affects": ["background"],
    "tone": "minimal",
    "theme": "light",
    "needs_sized_parent": false
  },
  "slot_hints": ["page-background", "section-background", "decoration"]
}
