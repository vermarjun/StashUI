{
  "name": "bg-singularity",
  "blurb": "Swirling geometric shader vortex pulsing with wave interference patterns.",
  "description": "ShaderToy shader rendering hypnotic singularity effect with interference patterns. Supports hue, saturation, brightness, and animation speed controls.",
  "use_when": "Sci-fi or psychedelic themed hero background needing extreme visual impact.",
  "avoid_when": "Photosensitive users, or when subtle background is needed.",
  "a11y": "Highly dynamic animation; consider prefers-reduced-motion fallback.",
  "props": [
    {"name": "hue", "type": "number", "default": null, "required": false},
    {"name": "saturation", "type": "number", "default": null, "required": false},
    {"name": "brightness", "type": "number", "default": null, "required": false},
    {"name": "speed", "type": "number", "default": null, "required": false},
    {"name": "className", "type": "string", "default": null, "required": false}
  ],
  "traits": {
    "trigger": ["always"],
    "motion": "pronounced",
    "affects": ["background"],
    "tone": "dramatic",
    "theme": "dark",
    "needs_sized_parent": false
  },
  "slot_hints": ["page-background", "hero", "section-background"]
}
