{
  "name": "ShapeBlur-TS-TW",
  "blurb": "Shape with a blur halo that follows the cursor.",
  "description": "Renders a geometric shape (rectangle, circle, or polygon) with a glowing blur halo that tracks pointer movement using shader sampling distance fields.",
  "use_when": "Immersive backgrounds or interactive focal points that react to pointer presence.",
  "avoid_when": "Touch-only devices (no pointer); requires a sized parent container.",
  "a11y": "Canvas wrapper only; no keyboard interaction or prefers-reduced-motion support.",
  "props": [
    {"name": "className", "type": "string", "default": "\"\"", "required": false},
    {"name": "variation", "type": "number", "default": "0", "required": false},
    {"name": "pixelRatioProp", "type": "number", "default": "2", "required": false},
    {"name": "shapeSize", "type": "number", "default": "1.2", "required": false},
    {"name": "roundness", "type": "number", "default": "0.4", "required": false},
    {"name": "borderSize", "type": "number", "default": "0.05", "required": false},
    {"name": "circleSize", "type": "number", "default": "0.3", "required": false},
    {"name": "circleEdge", "type": "number", "default": "0.5", "required": false}
  ],
  "traits": {
    "trigger": ["pointer-move"],
    "motion": "continuous",
    "affects": ["background"],
    "tone": "decorative",
    "theme": "both",
    "needs_sized_parent": true
  },
  "slot_hints": ["page-background", "hero", "section-background"]
}
