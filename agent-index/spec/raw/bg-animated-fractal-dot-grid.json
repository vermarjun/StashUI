{
  "name": "bg-animated-fractal-dot-grid",
  "blurb": "Canvas-rendered dot grid ripples outward from cursor with glowing wave effect.",
  "description": "Fullscreen canvas background with dot grid. On pointer movement, dots create radial wave with glow and optional noise overlay. Includes responsive performance scaling and mouse glow effect.",
  "use_when": "High-impact hero or section background needing smooth interactive pointer feedback.",
  "avoid_when": "Low-power devices, frequent window resizing, or minimal motion preferences.",
  "a11y": "Decorative animation tied to mouse movement; no keyboard or motion-reduced variant.",
  "props": [
    {"name": "dotSize", "type": "number", "default": "4", "required": false},
    {"name": "dotSpacing", "type": "number", "default": "20", "required": false},
    {"name": "dotOpacity", "type": "number", "default": "0.3", "required": false},
    {"name": "waveIntensity", "type": "number", "default": "30", "required": false},
    {"name": "waveRadius", "type": "number", "default": "200", "required": false},
    {"name": "dotColor", "type": "string", "default": "rgba(100, 100, 255, 1)", "required": false},
    {"name": "glowColor", "type": "string", "default": "rgba(100, 100, 255, 1)", "required": false},
    {"name": "enableNoise", "type": "boolean", "default": "true", "required": false},
    {"name": "noiseOpacity", "type": "number", "default": "0.03", "required": false},
    {"name": "enableMouseGlow", "type": "boolean", "default": "true", "required": false},
    {"name": "initialPerformance", "type": "\"low\" | \"medium\" | \"high\"", "default": "\"medium\"", "required": false}
  ],
  "traits": {
    "trigger": ["pointer-move"],
    "motion": "pronounced",
    "affects": ["background"],
    "tone": "dramatic",
    "theme": "dark",
    "needs_sized_parent": true
  },
  "slot_hints": ["page-background", "hero", "section-background"]
}
