{
  "name": "grid-pattern",
  "blurb": "Static SVG grid with optional square highlights.",
  "description": "SVG pattern overlay that renders a thin grid. Can highlight specific grid cells by passing an array of [x,y] coordinates.",
  "use_when": "A lightweight, static grid background that needs selective highlighting via the squares prop.",
  "avoid_when": "Heavy highlighting — each square is a separate SVG rect element, so hundreds of highlighted squares may impact performance.",
  "a11y": "Marked aria-hidden with pointer-events-none; purely decorative overlay.",
  "props": [
    {"name": "width", "type": "number", "default": "40", "required": false},
    {"name": "height", "type": "number", "default": "40", "required": false},
    {"name": "x", "type": "number", "default": "-1", "required": false},
    {"name": "y", "type": "number", "default": "-1", "required": false},
    {"name": "strokeDasharray", "type": "string", "default": "'0'", "required": false},
    {"name": "squares", "type": "Array<[x: number, y: number]>", "default": null, "required": false},
    {"name": "className", "type": "string", "default": null, "required": false}
  ],
  "traits": {
    "trigger": ["always"],
    "motion": "none",
    "affects": ["background"],
    "tone": "minimal",
    "theme": "light",
    "needs_sized_parent": false
  },
  "slot_hints": ["page-background", "section-background", "decoration"]
}
