{
  "name": "ScrambledText-TS-TW",
  "blurb": "Text scrambles with random characters as the cursor approaches, unscrambling when it leaves.",
  "description": "Uses GSAP with SplitText and ScrambleTextPlugin to split text into characters. Characters within a radius of the pointer animate to scrambled state, then back to normal as the pointer moves away.",
  "use_when": "An interactive text element needs to respond dynamically to cursor proximity with a glitch-like effect.",
  "avoid_when": "Touch-only interfaces or when rapid pointer tracking isn't supported; uses pointermove events.",
  "a11y": "Text is split into character spans and animated; meaning is preserved in data attributes. Not directly accessible during animation.",
  "props": [
    {"name": "radius", "type": "number", "default": "100", "required": false},
    {"name": "duration", "type": "number", "default": "1.2", "required": false},
    {"name": "speed", "type": "number", "default": "0.5", "required": false},
    {"name": "scrambleChars", "type": "string", "default": ".:,", "required": false},
    {"name": "className", "type": "string", "default": "", "required": false},
    {"name": "style", "type": "React.CSSProperties", "default": "{}", "required": false},
    {"name": "children", "type": "React.ReactNode", "default": null, "required": true}
  ],
  "traits": {
    "trigger": ["pointer-move"],
    "motion": "pronounced",
    "affects": ["text"],
    "tone": "playful",
    "theme": "both",
    "needs_sized_parent": false
  },
  "slot_hints": ["headline", "body-text"]
}
