{
  "name": "components-backgrounds-gradient",
  "blurb": "Gradient shifts smoothly through blue, purple, and pink in a loop.",
  "description": "Motion/react animated background using background-position animation to shift gradient colors. Blue→purple→pink loop with 15-second cycle.",
  "use_when": "Simple animated gradient background without configuration needs.",
  "avoid_when": "Contexts requiring theme customization or static backgrounds.",
  "a11y": "Decorative animation; no prefers-reduced-motion handling.",
  "props": [
    {"name": "className", "type": "string", "default": null, "required": false},
    {"name": "transition", "type": "HTMLMotionProps", "default": "{duration: 15, ease: 'easeInOut', repeat: Infinity}", "required": false}
  ],
  "traits": {
    "trigger": ["always"],
    "motion": "continuous",
    "affects": ["background"],
    "tone": "decorative",
    "theme": "dark",
    "needs_sized_parent": true
  },
  "slot_hints": ["page-background", "section-background", "hero"]
}
