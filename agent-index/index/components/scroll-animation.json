{
 "name": "scroll-animation",
 "title": "Scroll Animation Utility",
 "category": "effects",
 "origin": "Ui-Layouts",
 "origin_short": "uila",
 "exports": [
  "ScrollAnimation"
 ],
 "npm_deps": [
  "motion"
 ],
 "registry_deps": [],
 "css": false,
 "hardcoded_colors": 0,
 "flags": [
  "motion"
 ],
 "usage": "import { ScrollAnimation } from \"@/components/ui/scroll-animation\";\n\nconst cards = [\n  { title: \"Blur In\", body: \"Fades and unblurs as it enters the viewport — great for hero text or section intros.\" },\n  { title: \"Direction Control\", body: \"Animate from up, down, left, or right with a single prop change.\" },\n  { title: \"Staggered Delay\", body: \"Pass a delay value per element to choreograph multi-step entrance sequences.\" },\n  { title: \"Scroll Trigger\", body: \"Powered by Framer Motion's whileInView — fires once when 30 % of the element is visible.\" },\n];\n\nexport default function Demo() {\n  return (\n    <div className=\"w-full max-w-xl mx-auto py-12 px-4 flex flex-col gap-6\">\n      {cards.map((card, i) => (\n        <ScrollAnimation key={card.title} direction=\"up\" delay={i * 0.12}>\n          <div className=\"rounded-xl border border-border bg-muted/50 p-6\">\n            <p className=\"text-sm font-semibold text-foreground mb-1\">{card.title}</p>\n            <p className=\"text-sm text-muted-foreground leading-relaxed\">{card.body}</p>\n          </div>\n        </ScrollAnimation>\n      ))}\n    </div>\n  );\n}",
 "usage_source": "demo",
 "file": "registry/ui-layouts/scroll-animation.tsx",
 "install": "npx shadcn@latest add https://stashui.vercel.app/r/scroll-animation.json",
 "preview": "https://stashui.vercel.app/c/scroll-animation",
 "dupe_group": null,
 "alternatives": [],
 "blurb": "Fades and slides in from a direction when scrolled into view.",
 "description": "Wraps children in a motion component that triggers animation via scroll. Animates blur, opacity, and translate from a specified direction; fires once when 30% of element is visible.",
 "use_when": "Section intros or staggered card lists that should feel interactive as users scroll.",
 "avoid_when": "When users are on low-end devices or when motion would be distracting — no prefers-reduced-motion guard.",
 "a11y": "Content is not hidden from screen readers (always in DOM). Animation timing is not controllable by keyboard; only triggers on scroll.",
 "props": [
  {
   "name": "children",
   "type": "React.ReactNode",
   "default": null,
   "required": true
  },
  {
   "name": "className",
   "type": "string",
   "default": "{cn(className)}",
   "required": false
  },
  {
   "name": "variants",
   "type": "{hidden?: any; visible?: any}",
   "default": "{modifiedVariants}",
   "required": false
  },
  {
   "name": "viewport",
   "type": "{amount?: number; margin?: string; once?: boolean}",
   "default": "defaultViewport",
   "required": false
  },
  {
   "name": "delay",
   "type": "number",
   "default": "0",
   "required": false
  },
  {
   "name": "direction",
   "type": "'up' | 'down' | 'left' | 'right'",
   "default": "'down'",
   "required": false
  },
  {
   "name": "as",
   "type": "'div' | 'span' | 'h1' | 'h2' | 'h3' | 'a' | 'p' | 'section' | 'figure' | 'button' | 'article'",
   "default": "'div'",
   "required": false
  }
 ],
 "traits": {
  "trigger": [
   "scroll"
  ],
  "motion": "subtle",
  "affects": [
   "fill"
  ],
  "tone": "minimal",
  "theme": "both",
  "needs_sized_parent": false
 },
 "slot_hints": [
  "body-text",
  "feature-card",
  "list-item"
 ]
}