{
  "name": "@vermarjun/stashui-mcp",
  "version": "0.1.0",
  "description": "MCP server for the StashUI component registry — 1518 components, navigated rather than searched.",
  "type": "module",
  "bin": { "stashui-mcp": "dist/index.js" },
  "files": ["dist", "index"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "inspect": "npx @modelcontextprotocol/inspector node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": { "typescript": "^5.6.0", "@types/node": "^22.0.0" },
  "engines": { "node": ">=18" }
}
